import QuantumInfo.Finite.Entropy.VonNeumann

/-!
This compatibility module currently re-exports the basic finite-dimensional entropy interface only.

The previous file sketched strong subadditivity and its standard corollaries, but that development
was not yet backed by a complete proof stack. Those theorem shells have been removed from the
maintained surface until the required relative-entropy machinery is formalized end to end.
-/

noncomputable section
